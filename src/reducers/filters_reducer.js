const filtersReducerDefState = {
	text: '',
	sortBy: 'date',
	startDate: undefined,
	endDate: undefined
};

const filtersReducer = (state = filtersReducerDefState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
